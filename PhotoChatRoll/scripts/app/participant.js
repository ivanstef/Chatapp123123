/**
 * Users model
 */

var app = app || {};

app.Participant = (function () {
    'use strict';

    var participantModel = (function () {

        var current = {};

        return {
            current: current
        };

    }());

    return participantModel;

}());
